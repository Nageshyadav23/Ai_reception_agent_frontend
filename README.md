# AI Reception Agent – Frontend

The **AI Reception Agent Frontend** provides an intuitive web-based interface where users can interact with the virtual receptionist. The system allows visitors to speak in any language, ask queries, and receive immediate responses. It acts as the front desk for organizations to automate call reception, categorize queries, and ensure 24/7 availability.

---

## 🚀 Overview

This frontend serves as the user interaction layer of the AI Reception Agent system. It enables:

- Voice-based interaction with the receptionist
- Automatic conversion of user speech to text
- Display of responses generated by the backend agent
- Seamless communication with the backend API

The UI is lightweight, responsive, and accessible across devices.

---

## ✨ Features

- 🗣️ **Voice Input Support**  
  Users can speak in any language; the browser captures audio and sends it to the backend for speech-to-text processing.

- 💬 **Real-Time Query Responses**  
  The UI displays messages and suggestions provided by the AI agent.

- 🎯 **Categorized Handling of Requests**  
  Calls and messages are classified into categories like:  
  **HR**, **Support**, **Appointments**, and **General Enquiries**.

- 🌐 **Cross-platform Web Interface**  
  Works across desktop and mobile browsers.

- 🔗 **REST API Integration**  
  Communicates with the Node.js backend seamlessly.

---

## 🧱 Tech Stack

| Layer          | Technology     |
|----------------|---------------|
| Frontend       | React.js       |
| Styling        | CSS / Tailwind (if used) |
| Hosting        | Render.com     |
| Voice Support  | Web Speech API (Browser) |

---

## 🛠️ Setup & Run Instructions

1.create a folder by name "frontend"
2.move to that folder : cd frontend
3.clone the repo :git clone <your-frontend-repo-url>
4.Install dependencies : npm install
5.change package.json start value to "react-scripts start"
6.developmemt command : npm start
7.Access the app in "http://localhost:3000"

## Deployment link : https://ny-receptionagent.onrender.com

## How it works
  User → Speaks into Mic → Speech captured
  → Sent to backend → Processed by LLM → Response generated
  → Returned to frontend → Displayed to user


## Architectural Flow :

      User Speech
         ↓
    Frontend (React)
         ↓ sends audio
    Backend (Node.js)
         ↓ speech-to-text
    OpenAI LLM → classify query
         ↓
    Supabase → store logs
         ↓
    Response sent back to frontend

##🚀 Future Improvements

1. Add user-specific dashboards
2. Multi-language UI labels
3. Animated voice assistant avatar
4. Integration with CRM tools
5. Offline mode support



    




